

/**
 * PING:
 * Request -> ?
 * Response -> SN=XXXXXXX><>\r
*/

import { SerialNumberSchema } from "../../schemas"

export const ping = (text: string) => {
  const 
  const serialNumber = v.parse(SerialNumberSchema, )
}